import React, { useEffect, useState } from 'react';
import { ChevronRight, HelpCircle, Search, ShieldCheck, Sparkles, Star } from 'lucide-react';
import StorefrontChrome from './components/StorefrontChrome';
import { loadAdminCatalog } from './storefront/services/admin-catalog';

const BRAND = { bg: '#F7F8FC', line: '#E3E8F0', ink: '#161A22', muted: '#667487', primary: '#18A7D0', primaryDark: '#127B98', accent: '#7B3FE4' };
const sharedRelated = [{ title: 'Standard Business Cards', image: '/images/business-card-front.svg', price: 'From £21.99', path: '/business-cards/standard-business-cards' }, { title: 'Premium Flyers', image: '/images/flyer-front.svg', price: 'From £18.40', path: '/flyers' }, { title: 'Large Format Posters', image: '/images/poster-main.svg', price: 'From £8.49', path: '/posters-large-format-prints' }, { title: 'Wiro Bound Booklets', image: '/images/hero-slide-2.svg', price: 'Quote ready', path: '/booklets' }, { title: 'Labels & Stickers', image: '/images/hero-slide-3.svg', price: 'Quote ready', path: '/all-products' }];
const categoryData = { '/all-products': { eyebrow: 'All products', title: 'Browse print products by category, use case and support type.', intro: 'A fuller catalogue view for business cards, flyers, posters, booklets, signage, labels, stationery and custom quotes.', image: '/images/hero-slide-2.svg', breadcrumb: 'Home / Categories / All Products', groups: [{ title: 'Popular categories', description: 'The most common print products customers need first.', products: sharedRelated }], seoTitle: 'All print products', seoBody: 'The all-products page groups key print products into clear sections.', blogs: [{ title: 'Choosing print products for a launch', body: 'Start with cards, flyers, posters and signage.', image: '/images/hero-slide-2.svg' }, { title: 'When to request a quote', body: 'Custom sizes, large quantities and specialist materials should go through quote review.', image: '/images/hero-slide-3.svg' }], faqs: ['Can I order online?', 'Can I request a quote?', 'Can I upload artwork later?', 'Do you support business accounts?', 'Can I collect locally?'] } };
const categoryAliases = { '/business-cards': { ...categoryData['/all-products'], eyebrow: 'Business cards', title: 'Business card options for teams, brands and everyday networking.', intro: 'Compare standard, premium and related business stationery products before choosing a product or quote route.', image: '/images/business-card-front.svg', breadcrumb: 'Home / Categories / Business Cards', groups: [{ title: 'Business card products', description: 'Start with standard cards, then add finishes, corners or supporting stationery.', products: [{ title: 'Standard Business Cards', description: 'Everyday cards with clean stock and finish choices.', price: 'From £21.99', image: '/images/business-card-front.svg', path: '/business-cards/standard-business-cards' }] }] } };

export function isCategoryLandingRoute(pathname = '/') { const clean = String(pathname || '/').split('?')[0].replace(/\/$/, '') || '/'; return Boolean(categoryData[clean] || categoryAliases[clean]); }
function cleanPath(pathname = '/') { return String(pathname || '/').split('?')[0].replace(/\/$/, '') || '/'; }
function cleanSlug(pathname = '/') { return cleanPath(pathname).replace(/^\/+/, ''); }
function navigate(path) { window.history.pushState({}, '', path); window.dispatchEvent(new Event('locationchange')); window.scrollTo({ top: 0, behavior: 'smooth' }); }
function CategoryShell({ children, className = '' }) { return <div className={`mx-auto w-full max-w-[1360px] px-4 sm:px-6 lg:px-8 ${className}`}>{children}</div>; }

function adminPageFromCatalog(clean, catalog) {
  const slug = cleanSlug(clean);
  if (!slug || !catalog?.categories?.length) return null;
  const category = catalog.categories.find((item) => item.slug === slug || item.path === clean);
  if (!category) return null;
  const content = category.content || {};
  const products = catalog.products.filter((product) => product.categorySlug === category.slug);
  return { eyebrow: category.name, title: content.heroTitle || `${category.name} printing and product options`, intro: content.heroIntro || category.description || `Browse ${category.name} products from the admin catalogue.`, image: content.heroImage || category.thumbnail || products[0]?.image || '/images/hero-slide-2.svg', breadcrumb: `Home / Categories / ${category.name}`, groups: [{ title: category.name, description: category.description || `Products listed under ${category.name}.`, products: products.length ? products.map((product) => ({ title: product.title, description: product.description, price: product.price, image: product.image, path: product.path })) : sharedRelated }], seoTitle: content.seoTitle || `${category.name} printing`, seoBody: content.seoDescription || category.description || `${category.name} products are loaded from the admin catalogue.`, blogs: [{ title: `${category.name} artwork support`, body: 'Upload print-ready artwork or ask for help before checkout.', image: content.heroImage || category.thumbnail || '/images/hero-slide-1.svg' }, { title: 'Need a custom quote?', body: 'Use bespoke quote support for unusual sizes, materials or quantities.', image: '/images/hero-slide-3.svg' }], faqs: ['Can I order online?', 'Can I upload artwork later?', 'Can I request a quote?', 'Can I collect locally?', 'Can you help with design?'] };
}

function ProductTile({ item }) { return <button onClick={() => navigate(item.path)} className="group text-left"><div className="overflow-hidden rounded-[18px] border bg-white shadow-[0_14px_30px_rgba(0,0,0,0.04)] transition duration-200 group-hover:-translate-y-[2px] group-hover:shadow-[0_22px_48px_rgba(0,0,0,0.08)]" style={{ borderColor: BRAND.line }}><div className="bg-[#F4F7FA] p-3"><img src={item.image} alt={item.title} className="h-44 w-full rounded-[12px] object-cover" /></div><div className="p-4"><div className="text-[15px] font-black tracking-[-0.02em]" style={{ color: BRAND.ink }}>{item.title}</div><p className="mt-1 min-h-[38px] text-[12px] leading-5" style={{ color: BRAND.muted }}>{item.description}</p><div className="mt-3 flex items-center justify-between gap-3"><span className="text-[13px] font-bold" style={{ color: BRAND.ink }}>{item.price}</span><span className="inline-flex items-center gap-1 text-[12px] font-bold" style={{ color: BRAND.primary }}>View <ChevronRight className="h-4 w-4" /></span></div></div></div></button>; }
function ProductGroup({ group }) { return <section className="py-5"><CategoryShell><div className="mb-4"><h2 className="text-[26px] font-black tracking-[-0.035em]" style={{ color: BRAND.ink }}>{group.title}</h2><p className="mt-2 max-w-[860px] text-[13px] leading-6" style={{ color: BRAND.muted }}>{group.description}</p></div><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5">{group.products.map((item) => <ProductTile key={`${item.title}-${item.path}`} item={item} />)}</div></CategoryShell></section>; }
function BlogCard({ item }) { return <div className="rounded-[22px] border bg-white p-4 shadow-[0_14px_30px_rgba(0,0,0,0.04)]" style={{ borderColor: BRAND.line }}><img src={item.image} alt={item.title} className="h-56 w-full rounded-[16px] object-cover" /><div className="mt-4 text-[18px] font-black tracking-[-0.03em]" style={{ color: BRAND.ink }}>{item.title}</div><p className="mt-2 text-[12px] leading-6" style={{ color: BRAND.muted }}>{item.body}</p></div>; }

export default function CategoryLandingPage({ pathname = '/' }) {
  const clean = cleanPath(pathname);
  const [catalog, setCatalog] = useState(null);
  useEffect(() => { let active = true; loadAdminCatalog().then((data) => { if (active) setCatalog(data); }).catch(() => {}); return () => { active = false; }; }, []);
  const page = adminPageFromCatalog(clean, catalog) || categoryData[clean] || categoryAliases[clean] || categoryData['/all-products'];
  const related = sharedRelated.filter((item) => !page.groups.some((group) => group.products.some((product) => product.title === item.title))).slice(0, 5);
  return <StorefrontChrome currentPath={clean}><section className="border-b bg-white" style={{ borderColor: BRAND.line }}><CategoryShell className="py-10 lg:py-14"><div className="overflow-hidden rounded-[28px] border shadow-[0_22px_60px_rgba(0,0,0,0.06)]" style={{ borderColor: BRAND.line, background: `linear-gradient(135deg, ${BRAND.primary} 0%, ${BRAND.primaryDark} 58%, #EAF9FD 58%, #F8FCFF 100%)` }}><div className="grid gap-8 p-7 md:grid-cols-[0.95fr_1.05fr] md:p-10 lg:p-12"><div className="flex flex-col justify-center text-white"><div className="text-[10px] font-black uppercase tracking-[0.18em] text-white/80">{page.eyebrow}</div><h1 className="mt-4 max-w-[620px] text-[38px] font-black leading-[0.98] tracking-[-0.055em] md:text-[54px]">{page.title}</h1><p className="mt-5 max-w-[560px] text-[14px] leading-7 text-white/88">{page.intro}</p><div className="mt-7 flex flex-wrap gap-3"><button onClick={() => window.dispatchEvent(new CustomEvent('open-holo-search'))} className="inline-flex items-center gap-2 rounded-full bg-white px-5 py-3 text-[12px] font-black" style={{ color: BRAND.ink }}><Search className="h-4 w-4" />Search products</button><button onClick={() => navigate('/bespoke-quote')} className="rounded-full border border-white/45 px-5 py-3 text-[12px] font-black text-white">Request a quote</button></div></div><div className="flex items-center justify-center"><div className="relative w-full max-w-[560px] rounded-[26px] border border-white/50 bg-white/70 p-4 shadow-[0_28px_80px_rgba(0,0,0,0.16)] backdrop-blur"><img src={page.image} alt={page.title} className="h-[280px] w-full rounded-[18px] object-cover" /></div></div></div></div></CategoryShell></section><section className="py-4"><CategoryShell><div className="text-[11px]" style={{ color: BRAND.muted }}>{page.breadcrumb}</div></CategoryShell></section>{page.groups.map((group) => <ProductGroup key={group.title} group={group} />)}<section className="py-8"><CategoryShell><div className="mx-auto max-w-[860px] text-center"><div className="text-[10px] font-black uppercase tracking-[0.18em]" style={{ color: BRAND.primary }}>{page.eyebrow} guidance</div><h2 className="mt-3 text-[30px] font-black tracking-[-0.04em]" style={{ color: BRAND.ink }}>{page.seoTitle}</h2><p className="mt-4 text-[13px] leading-7" style={{ color: BRAND.muted }}>{page.seoBody}</p></div></CategoryShell></section><section className="py-6"><CategoryShell><div className="mb-5 text-center text-[26px] font-black tracking-[-0.04em]" style={{ color: BRAND.ink }}>{page.eyebrow} help and ideas</div><div className="grid gap-5 md:grid-cols-2">{page.blogs.map((item) => <BlogCard key={item.title} item={item} />)}</div></CategoryShell></section><section className="py-6"><CategoryShell><div className="rounded-[24px] border bg-white p-6 shadow-[0_14px_30px_rgba(0,0,0,0.04)]" style={{ borderColor: BRAND.line }}><div className="text-center"><div className="text-[10px] font-black uppercase tracking-[0.18em]" style={{ color: BRAND.primary }}>Frequently asked questions</div><h2 className="mt-3 text-[28px] font-black tracking-[-0.04em]" style={{ color: BRAND.ink }}>Common questions before customers order</h2></div><div className="mx-auto mt-6 max-w-[980px] divide-y" style={{ borderColor: BRAND.line }}>{page.faqs.map((question) => <button key={question} className="flex w-full items-center justify-between gap-4 py-4 text-left text-[14px] font-semibold" style={{ color: BRAND.ink }}><span>{question}</span><HelpCircle className="h-4 w-4 shrink-0" style={{ color: BRAND.primary }} /></button>)}</div></div></CategoryShell></section><section className="py-6"><CategoryShell><div className="mb-5 text-center text-[26px] font-black tracking-[-0.04em]" style={{ color: BRAND.ink }}>Customers also order</div><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5">{related.map((item) => <ProductTile key={item.title} item={{ ...item, description: 'Often ordered with this category for a complete print bundle.' }} />)}</div></CategoryShell></section><section className="py-8"><CategoryShell><div className="grid gap-4 md:grid-cols-3">{[['Artwork check included', 'Upload print-ready files or request help before production.', ShieldCheck], ['Business account ready', 'Useful for repeat jobs, local teams and larger orders.', Star], ['Custom quote route', 'Move complex sizes, materials and finishing into quote review.', Sparkles]].map(([title, body, Icon]) => <div key={title} className="rounded-[20px] border bg-white p-5 shadow-[0_12px_28px_rgba(0,0,0,0.035)]" style={{ borderColor: BRAND.line }}><Icon className="h-5 w-5" style={{ color: BRAND.primary }} /><div className="mt-4 text-[15px] font-black" style={{ color: BRAND.ink }}>{title}</div><p className="mt-2 text-[12px] leading-6" style={{ color: BRAND.muted }}>{body}</p></div>)}</div></CategoryShell></section></StorefrontChrome>;
}
